# .ebextensions/instance_profile.config

option_settings:
  aws:autoscaling:launchconfiguration:
    IamInstanceProfile: "eb_instance_profile"  # Ensure this matches the instance profile name created
